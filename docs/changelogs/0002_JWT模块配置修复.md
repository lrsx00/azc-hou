# 变更日志: JWT模块配置修复
**日期**: 2025-12-09
**任务**: 修复 Jwt 模块工厂函数类型问题并提供默认配置

## 📂 文件变更
### 修改 (Modified) / 新增 (New)
- `[src/auth/auth.module.ts]`:
  - 将 `JwtModule.registerAsync` 的工厂改为同步函数，显式返回 `JwtModuleOptions`，并为 `secret` 与 `expiresIn` 增加默认值以消除类型报错。

## 💡 技术说明 (Technical Notes)
- **关键点**: 同步工厂函数返回显式类型，避免 `ConfigService.get` 返回 string 与 Jwt 11 类型不兼容的报错。
- **配置变更**: 无新增配置（`.env` 未改动）。

