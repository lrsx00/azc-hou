# 变更日志: 开启本地前端CORS访问
**日期**: 2025-12-09
**任务**: 为本地前端 (http://localhost:5173) 启用 CORS 访问后端

## 📂 文件变更
### 修改 (Modified) / 新增 (New)
- `[src/main.ts]`:
  - 添加 `app.enableCors`，仅允许 `http://localhost:5173`，并配置方法、头、凭据；保留全局校验管道与端口读取逻辑。

## 💡 技术说明 (Technical Notes)
- **关键点**: 单点启用 CORS，避免重复配置；确保仅本地前端可访问并允许携带凭据。
- **配置变更**: 无新增配置（`.env` 未改动）。

