# 变更日志: JWT策略密钥默认值修复
**日期**: 2025-12-09
**任务**: 修复 JwtStrategy secretOrKey 可能为 undefined 的类型错误

## 📂 文件变更
### 修改 (Modified) / 新增 (New)
- `[src/auth/strategies/jwt.strategy.ts]`:
  - 在构造函数中为 `secretOrKey` 提供默认值 `default_jwt_secret`，避免配置缺失导致的类型错误。

## 💡 技术说明 (Technical Notes)
- **关键点**: 通过提前获取并兜底 `secret`，确保 Passport JWT 策略的配置满足类型要求。
- **配置变更**: 无新增配置（`.env` 未改动）。

